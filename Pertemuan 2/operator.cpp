#include <iostream>
using namespace std;

int main() {
    int a, b;

    system("CLS");

    // assigment operator, " = ", ngasi nilai ke dalam suatu variabel
    a = 3;
    b = 5;

//     // arithmetical operator, " + - * / % ", untuk melakukan operasi matematika
//     int tambah = a + b;
//     int kurang = a - b;
//     int kali = a * b;
//     // float bagi = a / b;
//     float bagi = (float) a / (float) b; // untuk mendapatkan hasil pembagian yang lebih akurat, kita bisa melakukan type casting
//     int modulo = a % b;

//     cout << "Hasil Penjumlahan : " << tambah << endl;
//     cout << "Hasil Pengurangan : " << kurang << endl;
//     cout << "Hasil Perkalian : " << kali << endl;
//     cout << "Hasil Pembagian : " << bagi << endl;
//     cout << "Hasil sisa bagi : " << modulo << endl;

    // relational operator, " > < >= <= == != ", untuk membandingkan dua nilai, hasil berupa boolean (true/false)
//     cout << (a == b) << endl;
//     cout << (a < b) << endl;
//     cout << (a != b) << endl;

    // logical operator, " && || ! ", untuk menggabungkan beberapa kondisi, hasil berupa boolean (true/false)
    
    // cout << ( true && true ) << endl;
    // cout << ( true && false ) << endl;

    // cout << (true || false) << endl; 
    // cout << (false || false) << endl;

    // cout << !true << endl;
    // cout << !false << endl;

    // bitwise operator, " & | ^ ~ << >> ", untuk melakukan operasi bitwise pada bilangan biner

    // cout << (5 & 7) << endl; // 5 = 101, 7 = 111, hasilnya 101 = 5
    // cout << (5 | 7) << endl; // 5 = 101, 7 = 111, hasilnya 111 = 7
    // cout << (5 ^ 7) << endl; // 5 = 101, 7 = 111, hasilnya 010 = 2
    // cout << (~7) << endl; // 7 = 0000 0111, hasilnya 1111 1000 = -8 (karena menggunakan sistem bilangan komplemen dua)
    // cout << (7 << 2) << endl;
    // cout << (7 >> 2) << endl;

    // Operator Kondisional (Ternary Operator), " ? : ", untuk membuat kondisi yang lebih singkat ( kondisi -> hasil1 ? hasil2 )
    // int max;
    // max = (a > b) ? a : b;
    // cout << "Nilai terbesar adalah : " << max << endl;

    // Shorthand 
    // a += 7; // a = a + 7 ,
    // cout << a << endl;

    // increament and decrement operator, " ++ -- ", untuk menambah atau mengurangi nilai suatu variabel sebanyak 1
    // Pre Increament, ++ ada didepan variabel 
    // cout << a << endl; // 3
    // cout << ++a << endl; // 4, karena a ditambah 1 dulu baru ditampilkan

    // Post Increament, ++ ada dibelakang variabel
    // cout << a << endl; // 3
    // cout << a++ << endl; // 3, karena a ditampilkan dulu baru ditambah 1
    // cout << a << endl; // 4, karena a sudah ditambah 1 pada baris sebelumnya

    // Pre Decrement, -- ada didepan variabel
    // cout << b << endl; // 5
    // cout << --b << endl; // 4, karena b dikurangi 1 dulu baru ditampilkan

    //post decrement, -- ada dibelakang variabel
    // cout << b << endl;
    // cout << b-- << endl;
    // cout << b << endl;

    
  }